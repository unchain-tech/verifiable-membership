import { FC } from 'react';
import Layout from '../components/layouts';

export const Home: FC = () => {
  return (
    <Layout pageTitle="Home">
      <div id="body">
        <h1>UNCHAIN Membership App</h1>
      </div>
    </Layout>
  );
};

export default Home;
